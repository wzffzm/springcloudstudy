spring:
  cloud:
    config:
      name: configclient
      label: main
      discovery:
        enabled: true
        service-id: configserver
eureka:
  instance:
    prefer-ip-address: true
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka
